{{#if resume.awards.length}}
  <section class="section">
    <header>
      <h3 class="section-title">Toewijzingen</h2>
      <div class="bar"></div>
    </header>
    <section id="awards">
      {{#each resume.awards}}
        <section class="award-item">
          {{#if summary}}
            <label for="award-item-{{@index}}"></label>
          {{/if}}

          <header class="clear">
            {{#if date}}
              <div class="date">
                {{Y date}}
              </div>
            {{/if}}
            <div class="header-left">
              {{#if title}}
                <div class="title">
                  {{title}}
                </div>
              {{/if}}
              {{#if awarder}}
                <div class="awarder">
                  {{awarder}}
                </div>
              {{/if}}
            </div>
          </header>

          <div class="item">
            {{#if summary}}
              <div class="summary">{{paragraphSplit summary}}</div>
            {{/if}}
          </div>
        </section>
      {{/each}}
    </section>
  </section>
{{/if}}
